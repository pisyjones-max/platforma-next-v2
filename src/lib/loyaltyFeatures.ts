// Переключатели акций карты лояльности.
//
// Как включить/выключить фичу БЕЗ админки:
// 1. Открой этот файл на github.com (кнопка-карандаш "Edit this file").
// 2. Поменяй true/false или число.
// 3. Commit changes → commit прямо в main.
// 4. GitHub Actions задеплоит изменение на сервер автоматически (тот же пайплайн,
//    что и на любой обычный push) — ничего руками на VDS делать не нужно.
//
// Никакой сборки, npm install и т.п. не требуется — правишь текстовый файл в браузере.

export const LOYALTY_FEATURES = {
  // 0.5% → 1% → 1.5% кэшбэка после 1-й / 2-й / 3-й покупки по карте (см. CASHBACK_TIERS)
  progressiveCashback: true,
  // Начисление баллов за фото/видео-отзыв — начисляет менеджер вручную через /admin/cards (action=add)
  reviewBonus: true,
  // "Приведи соседа": реферер + приглашённый получают баллы при выпуске новой карты по ref-коду
  referralProgram: true,
  // Баллы "сгорают", если по карте нет активности (новая покупка) дольше POINTS_EXPIRY_DAYS
  pointsExpiry: true,
}

// Кэшбэк по номеру покупки (0-indexed: 1-я покупка = tier[0]). Дальше держится на последнем значении.
export const CASHBACK_TIERS = [0.005, 0.01, 0.015]

// Сколько баллов начисляет менеджер за принятый отзыв (диапазон — менеджер решает сам в этих рамках)
export const REVIEW_BONUS_MIN = 500
export const REVIEW_BONUS_MAX = 1000

// Баллы за реферальную программу — начисляются и рефереру, и новому клиенту
export const REFERRAL_BONUS_POINTS = 2000

// Через сколько дней неактивности баллы считаются сгоревшими (мягкая проверка при обращении,
// без фонового кроном пересчёта — баланс "оживает" на 0 при следующей проверке/начислении)
export const POINTS_EXPIRY_DAYS = 90
